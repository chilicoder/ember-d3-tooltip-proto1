
import { d3On } from 'ember-d3-chart-proto1/helpers/d3-on';
import { module, test } from 'qunit';

module('Unit | Helper | d3 on');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = d3On([42]);
  assert.ok(result);
});

