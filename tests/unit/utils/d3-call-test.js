import d3Call from 'ember-d3-chart-proto1/utils/d3-call';
import { module, test } from 'qunit';

module('Unit | Utility | d3 call');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = d3Call();
  assert.ok(result);
});
